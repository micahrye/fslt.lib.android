<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/androidLightBlue"
    android:orientation="horizontal"
    android:weightSum="5" >

    <edu.mit.media.affect.storyscape.remix.EditableImageView
        android:id="@+id/edit_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_weight="1"
        android:contentDescription="@string/edit_image"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_weight="4"
        android:background="@color/smokedGlass"
        android:gravity="left"
        android:orientation="vertical" >

        <Button
            android:id="@+id/undo_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/undo_btn"
            android:text="@string/undo_btn"
            android:textColor="@color/black" />

        <Button
            android:id="@+id/redo_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/redo_btn"
            android:text="@string/redo_btn" 
            android:textColor="@color/black"/>

        <RadioGroup
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <RadioButton
                android:id="@+id/radio_cut"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cut" 
                android:textColor="@color/black"/>

            <RadioButton
                android:id="@+id/radio_erase"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/erase" 
                android:textColor="@color/black"/>
        </RadioGroup>

        <Button
            android:id="@+id/edit_done_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/edit_done_btn"
            android:text="@string/edit_done_btn" 
            android:textColor="@color/black"/>
    </LinearLayout>

</RelativeLayout>